<!doctype html>
<html>
<head>
<title>Submerge</title>
</head>
<body>
<h1>Submerge</h1>
<p>Enter a URL for a YouTube channel and you'll get an RSS feed to subscribe to the
corresponding channel with HookTube.</p>
<form action="/submerge" method="post">
<input name="url" type="text">
<button type="submit">Submit</button>
% if (session 'channels') {
    % my $url = $config->{base_url} . '/feeds/videos.xml?channel_id=' . join(',', @{ session 'channels' });
    <a href="<%= $url %>"><%= $url %></a>
    <br>
    Enter more channel URLs to add to your feed.
% }
</form>
</body>
</html>
